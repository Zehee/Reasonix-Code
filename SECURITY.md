# 安全政策

## 报告安全漏洞

请勿通过公开 Issue 报告安全漏洞。

- **邮件**：通过 GitHub 安全顾问功能（Security Advisories）提交。
- **内容**：清晰描述影响范围、复现步骤、建议修复。
- **响应**：维护者将在 72 小时内确认并开始处理。

## 依赖安全

- 上游依赖已知漏洞通过 Dependabot 监控。
- 升级依赖时需通过 `npm run verify` 全量校验。

## 沙箱约束

文件系统工具受项目根目录（沙箱）约束：
- 读取/写入超出沙箱路径需显式审批（`path_access` gate）。
- 新建文件会解析最近存在祖先目录的真实路径（`realpathForNewPath`），防止符号链接跨沙箱绕过。