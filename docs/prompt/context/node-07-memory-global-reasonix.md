# Node 7 · 记忆栈 · Global memory (~/.reasonix/REASONIX.md)

## 来源

`src/memory/user.ts:333-349`

## 说明

跨项目固定笔记（`#g` 前缀写入），8000 字符截断。

## 原文（中文翻译稿，供对照）

> 动态模板。`applyGlobalReasonixMemory` 在节点 6 之后追加（如果 `~/.reasonix/REASONIX.md` 存在）。`${basePrompt}` 同前，`${mem.content}` 是文件正文（8000 字符截断）。用户通过 `#g` 前缀追加跨项目笔记。
>
> 原文——
>
> > # 全局记忆（~/.reasonix/REASONIX.md）
> >
> > 用户通过 `#g` 提示前缀固定的跨项目笔记。视为权威——与项目记忆同等信任级别。
> >
> > ```
> > ${mem.content}
> > ```

## v2

_（待细化）_
